public class Test_14 {

    - A `char` array `dest` is initialized with space for 50 characters.
    - `strcpy(dest, "Hello ")` copies the string `"Hello "` into `dest`.
    - `strcat(dest, src)` appends the string `src` (i.e., `"World"`) to `dest`.
    - The final string becomes `"Hello World"`.
    - The length of the string is printed using `%lu`, which in C is used for an `unsigned long`.
    
    ---
    
    ### 🔄 **Java Equivalent Translation:**

}