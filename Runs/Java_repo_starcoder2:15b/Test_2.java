public class Test_2 {

    // FAILED_EXTRACTION: LLM returned empty response.

    public static void move(Point p) {
      p.x += 5;
    }

    // FAILED_EXTRACTION: LLM returned empty response.

    // FAILED_EXTRACTION: LLM returned empty response.

}