public class Test_7 {

    // FAILED_EXTRACTION: LLM returned empty response.

    int subtract(int a, int b) { return a - b; }
          
          void main() {
            printf("subtract(%d, %d): %d\n", 345, 123, subtract(345, 123)); 
            printf("subtract(%d, %d): %d\n", -123, 456, subtract(-123, 456));
          }

    // FAILED_EXTRACTION: LLM returned empty response.

    // FAILED_EXTRACTION: LLM returned empty response.

}